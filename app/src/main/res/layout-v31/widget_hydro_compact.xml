<?xml version="1.0" encoding="utf-8"?>
<!-- Compact Widget Layout (1x1 default, resizable) - Material 3 Design -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.Material3.DynamicColors.DayNight"
    android:background="@drawable/widget_background_shape"
    android:backgroundTint="?attr/colorSurface"
    android:padding="@dimen/widget_padding_compact">

    <!-- Circular Progress Indicator with Percentage -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <ProgressBar
            android:id="@+id/widget_progress_circle"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:progressDrawable="@drawable/widget_circular_progress_small"
            android:progressTint="?attr/colorPrimary"
            android:progressBackgroundTint="?attr/colorSurfaceVariant"
            android:max="100"
            android:progress="0"
            android:rotation="-90" />

        <!-- Percentage Text (always visible) -->
        <TextView
            android:id="@+id/widget_progress_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="0%"
            android:textColor="?attr/colorOnSurface"
            android:textSize="12sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false" />

    </FrameLayout>

    <!-- Progress Text (hidden in 1x1, visible when resized) -->
    <TextView
        android:id="@+id/widget_progress_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:text="0L / 2.7L"
        android:textColor="?attr/colorOnSurface"
        android:textSize="@dimen/widget_text_caption"
        android:fontFamily="sans-serif"
        android:textAlignment="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:visibility="gone" />

    <!-- Last Updated (hidden by default) -->
    <TextView
        android:id="@+id/widget_last_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to open"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="@dimen/widget_text_caption"
        android:fontFamily="sans-serif"
        android:textAlignment="center"
        android:alpha="0.7"
        android:maxLines="1"
        android:visibility="gone" />

</FrameLayout>
